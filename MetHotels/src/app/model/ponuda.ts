export class Ponuda {
    naziv: string;
    popustPonude: number;
    
    constructor(naziv: string, popustPonude: number) {
        this.naziv = naziv;
        this.popustPonude = popustPonude;
    }
}